# 环境变量示例（PowerShell）
#
# 重要：本项目不会自动读取 .env 文件。请在当前会话中直接设置环境变量，
# 例如：$env:OPENAI_API_KEY = "..."
# 或者在图形界面的“模型设置”中创建 profile 并只写凭据。
#
# Legacy CLI fallback（未配置任何 profile 时使用）：
#   OPENAI_API_KEY 与 OPENAI_MODEL 为必需；OPENAI_BASE_URL 为可选。
#   有可用 profile 时，优先级为：显式 profile > 当前激活 profile > OPENAI_*。
OPENAI_API_KEY=
OPENAI_MODEL=
OPENAI_BASE_URL=

# 用户级配置目录（默认 ~/.coding-agent）。config.json 与 credentials.json
# 保存在这里，不会写入工作区。
# CODING_AGENT_HOME=C:/Users/you/.coding-agent

# 凭据引用也可由环境变量提供：ref "openai" 映射到 OPENAI_API_KEY，
# 其他 ref <name> 映射到 CODING_AGENT_CRED_<NAME>。
# 环境变量命中时，GUI 对该凭据的写入会被禁用（只读）。
# CODING_AGENT_CRED_DEEPSEEK=
