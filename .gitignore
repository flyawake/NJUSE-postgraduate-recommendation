# Credentials and local environment
.env
.env.*
!.env.example

# Python caches and build output
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.tox/
.coverage
htmlcov/

# Virtual environments
.venv/
venv/

# Node (frontend build tooling only; built static assets are committed)
node_modules/
playwright-report/
test-results/
frontend/coverage/

# Original task materials (must never be committed)
*.pdf

# Handoff / scratch materials (must never be committed)
tmp/
