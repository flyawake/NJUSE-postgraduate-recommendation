# task_004 production UI evidence

The PNG files in this directory are generated by `npm run test:e2e` against
the production Vite bundle, the real FastAPI server, and the local fake model.

They cover the conversation transcript/change summary, conditional file
preview, destructive-delete confirmation, 320 px English drawer, 1280×720
dark settings, and hard-process-restart recovery.
