# Guide 归档说明

- 已完成并验收通过的任务，由主规划 agent 将整个 `guide/task_XXX/` 文件夹移动到 `guide/archive/task_XXX/`。
- 归档前必须确认验收项全部通过、对应反馈已评估，且任务与反馈的索引关联完整。
- 归档后需在 `guide/INDEX.md` 中将任务状态更新为 `已归档`（archived），并将相关链接更新为归档路径。
- 归档内容保持只读，不得删除、覆盖、重命名或复用任务编号；后续修订应创建新任务并引用原任务。
